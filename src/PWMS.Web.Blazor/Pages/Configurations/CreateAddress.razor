@page "/configuration/address/create"
@attribute [Authorize]

<PageTitle>Create Address</PageTitle>

<MudContainer Class="p-1 m-0">
    <MudText Typo="Typo.h6">Create Address</MudText>
    <MudGrid>
        <MudItem xs="12" sm="7">
            <MudPaper Class="p-1">
                <MudForm Model="_createAddressDto" @ref="_form" @bind-IsValid="@success">
                    <MudTextField @bind-Value="_createAddressDto.AddressLine"
                                  Immediate="true"
                                  Label="Address Line"
                                  Validation="_addressLineValidator.Validation"
                                  OnKeyDown="FormKeyDownAsync"
                                  Required />

                    <MudCardActions Class="gap-2">
                        <MudSpacer />
                        <MudLoadingButton Variant="Variant.Filled"
                                          Color="Color.Primary"
                                          Class="ml-auto"
                                          Disabled="@(!success)"
                                          OnClick="Create"
                                          @bind-Loading="_loading">Create</MudLoadingButton>
                        <MudButton Color="Color.Warning"
                                   OnClick="Cancel">Cancel</MudButton>
                    </MudCardActions>
                </MudForm>
            </MudPaper>
        </MudItem>
    </MudGrid>
</MudContainer>